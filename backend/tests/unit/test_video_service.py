import pytest
# TODO: Import video service once created

@pytest.mark.asyncio
async def test_process_video(mock_firebase):
    # TODO: Implement once VideoService is created
    # service = VideoService(storage=mock_firebase)
    # result = await service.process_video("test_id")
    # assert result.status == "completed"
    pass

@pytest.mark.asyncio
async def test_video_metadata_extraction(mock_firebase):
    # TODO: Implement once VideoService is created
    # service = VideoService(storage=mock_firebase)
    # metadata = await service.extract_metadata("test_video.mp4")
    # assert metadata.duration > 0
    # assert metadata.format == "mp4"
    pass 